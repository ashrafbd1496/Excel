প্রতিদিনের প্রজেক্ট ম্যানেজমেন্ট এবং কোডিংয়ে সবচেয়ে বেশি ব্যবহৃত প্রয়োজনীয় Git কমান্ডগুলো নিচে ক্যাটাগরি অনুযায়ী সাজিয়ে দেওয়া হলো:

১. প্রজেক্ট সেটআপ ও রিপোজিটরি কনফিগারেশন

`git init` কাজ: বর্তমান ফোল্ডারটিকে একটি নতুন Git রিপোজিটরি হিসেবে ইনিশিয়ালাইজ (শুরু) করে।

`git clone <**URL**>` কাজ: GitHub বা রিমোট সার্ভার থেকে কোনো বিদ্যমান প্রজেক্ট পুরো হিস্ট্রি সহ লোকাল কম্পিউটারে নামিয়ে আনে।

`git remote -v` কাজ: লোকাল প্রজেক্টটি কোন অনলাইন/রিমোট রিপোজিটরির (Origin) সাথে যুক্ত আছে তা যাচাই করে।

২. দৈনন্দিন কাজের সাইকেল (Save & Commit)

`git status` কাজ: কোন কোন ফাইলে পরিবর্তন হয়েছে, কোনগুলো Staging Area-তে আছে বা আনট্র্যাকড অবস্থায় আছে তা দেখায়।

`git add .` কাজ: বর্তমান ফোল্ডারের সব পরিবর্তিত ও নতুন ফাইলকে পরবর্তী কমিটের জন্য Staging Area-তে যুক্ত করে। নির্দিষ্ট ফাইলের জন্য: `git add filename.ext`

`git commit -m *আপনার মেসেজ*` কাজ: Staging Area-তে থাকা পরিবর্তনগুলোর একটি স্ন্যাপশট বা ভার্সন সেভ করে।

`git diff` কাজ: ফাইলে ঠিক কোন কোন লাইন যোগ বা বিয়োগ করা হয়েছে তা বিস্তারিত দেখায় (কমিট করার আগে)।

৩. রিমোট সিঙ্ক (Push, Pull & Fetch)

`git push origin <branch-name>` কাজ: লোকাল কমিটগুলো GitHub বা রিমোট রিপোজিটরিতে আপলোড করে। প্রথমবার পুশ করতে `-u` ফ্ল্যাগ ব্যবহার করা হয় (`git push -u origin main`)।

`git pull origin <branch-name>` কাজ: রিমোট রিপোজিটরি থেকে সর্বশেষ কোড নামিয়ে স্বয়ংক্রিয়ভাবে লোকাল ব্রাঞ্চের সাথে মার্জ (Merge) করে।

`git fetch` কাজ: রিমোটের নতুন আপডেটগুলো ডাউনলোড করে কিন্তু লোকাল কোডের সাথে সরাসরি মার্জ করে না (শুধুমাত্র ট্র্যাকিংয়ের জন্য)।

৪. ব্রাঞ্চিং ও মার্জিং (Branch Management)

`git branch` কাজ: লোকাল রিপোজিটরিতে থাকা সমস্ত ব্রাঞ্চের তালিকা দেখায় এবং বর্তমানে কোনটিতে আছেন তা চিহ্নিত করে।

`git checkout -b <new-branch>` অথবা `git switch -c <new-branch>` কাজ: নতুন একটি ব্রাঞ্চ তৈরি করে সাথে সাথেই সেই ব্রাঞ্চে সুইচ করে।

`git checkout <branch-name>` অথবা `git switch <branch-name>` কাজ: একটি নির্দিষ্ট ব্রাঞ্চ থেকে অন্য ব্রাঞ্চে পরিবর্তন করে।

`git merge <branch-name>` কাজ: অন্য কোনো ব্রাঞ্চের করা কোড পরিবর্তন বর্তমান সক্রিয় ব্রাঞ্চের সাথে একত্রিত (Merge) করে।

৫. ভুল সংশোধন ও হিস্ট্রি দেখা (Undo & History)

`git log --oneline` কাজ: পূর্বের সমস্ত কমিট হিস্ট্রি এক লাইনে সংক্ষেপে দেখায়।

`git restore <filename>` কাজ: ফাইলে করা সাম্প্রতিক আনকমিটেড পরিবর্তনগুলো বাতিল করে ফাইলকে আগের অবস্থায় ফিরিয়ে নেয়।

`git stash` কাজ: বর্তমান অসম্পূর্ণ কাজগুলো কমিট না করে সাময়িকভাবে একটি গোপন স্থানে সেভ করে কোড ক্লিন রাখে।

`git stash pop` কাজ: `git stash`-এ জমিয়ে রাখা কাজগুলো আবার ফিরিয়ে আনে।

Git Stash হলো গিট-এর একটি সাময়িক ড্রয়ার বা ক্লিপবোর্ডের মতো।

আপনি যখন কোনো ফিচারে কাজ করছেন কিন্তু কাজটি এখনো পুরোপুরি শেষ হয়নি (কমিট করার উপযোগী না), ঠিক তখনই জরুরি প্রয়োজনে অন্য ব্রাঞ্চে যেতে হলে বা মেইন ব্রাঞ্চ থেকে নতুন আপডেট পুল করতে হলে `git stash` ব্যবহার করা হয়। এটি আপনার অসম্পূর্ণ পরিবর্তনগুলোকে নিরাপদে লুকিয়ে রেখে ওয়ার্কিং ডিরেক্টরি একদম ক্লিন করে দেয়।

একটি বাস্তব পরিস্থিতির উদাহরণ:

> ধরুন, আপনি `feature-login` ব্রাঞ্চে লগইন বাটন ডিজাইন করছেন। অর্ধেক কাজ হয়েছে, এমন সময় ক্লায়েন্ট বলল `main` ব্রাঞ্চে একটি জরুরি বাগ ফিক্স করতে হবে। > আপনি অসম্পূর্ণ ভাঙা কোড কমিট করতে চান না, আবার ফেলে দিতেও চান না। সমাধান: Git Stash।

1. অসম্পূর্ণ কাজ সাময়িক সেভ করে সরিয়ে রাখা:
লগইন বাটনের কাজটিকে একটি ডেসক্রিপশন সহ স্ট্যাশে লুকিয়ে ফেলুন:

```bash git stash save *wip: login button half done* # অথবা সংক্ষেপে: git stash -u

```

(নোট: `-u` বা `--include-untracked` ফ্ল্যাগ দিলে নতুন তৈরি করা ফাইলগুলোও স্ট্যাশে জমা হয়।)

2. জরুরি কাজটি শেষ করা:
এখন আপনার ওয়ার্কিং ডিরেক্টরি একদম ফ্রেশ। মেইন ব্রাঞ্চে গিয়ে বাগ ফিক্স করুন:

```bash git switch main # বাগ ফিক্স কোড লিখুন... git add . git commit -m *fix: critical payment bug* git push origin main

```

3. পূর্বের ফিচারে ফিরে আসা:
বাগ ফিক্স শেষ করে আবার আপনার আগের ব্রাঞ্চে ফিরে যান:

```bash git switch feature-login

```

4. স্ট্যাশ করা কাজ ফিরিয়ে আনা:
লুকিয়ে রাখা কোড আবার ওয়ার্কিং ডিরেক্টরিতে ফিরিয়ে আনুন এবং স্ট্যাশ তালিকা থেকে মুছে ফেলুন:

```bash git stash pop

```

Git Stash সম্পর্কিত প্রয়োজনীয় কমান্ডগুলোর তালিকা

| কমান্ড | বিবরণ |
|  |  |
| `git stash` | পরিবর্তিত সব ট্র্যাকড ফাইল ড্রয়ারে তুলে রাখে। |
| `git stash -u` | ট্র্যাকড এবং আনট্র্যাকড (নতুন তৈরি ফাইল) সব একসাথে স্ট্যাশ করে। |
| `git stash save *মেসেজ*` | মনে রাখার সুবিধার জন্য একটি নাম বা মেসেজ দিয়ে স্ট্যাশ করে। |
| `git stash list` | পূর্বে স্ট্যাশ করা সমস্ত আইটেমের তালিকা (`stash@{0}`, `stash@{1}`) দেখায়। |
| `git stash pop` | সর্বশেষ স্ট্যাশ ফিরিয়ে আনে এবং স্ট্যাশ লিস্ট থেকে সেটি ডিলিট করে দেয়। |
| `git stash apply` | স্ট্যাশ ফিরিয়ে আনে কিন্তু ভবিষ্যতে ব্যবহারের জন্য লিস্টে রেখে দেয়। |
| `git stash apply stash@{1}` | তালিকার নির্দিষ্ট কোনো ইনডেক্সের স্ট্যাশ ফিরিয়ে আনে। |
| `git stash show -p` | স্ট্যাশের ভেতরে কোন কোন ফাইলে কী পরিবর্তন ছিল তার diff দেখায়। |
| `git stash drop stash@{0}` | নির্দিষ্ট একটি স্ট্যাশ ডিলিট করে দেয়। |
| `git stash clear` | স্ট্যাশ হিস্ট্রি থেকে সমস্ত সেভ করা ডেটা মুছে ফেলে। |